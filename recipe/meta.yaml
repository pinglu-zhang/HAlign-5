package:
  name: halign5
  version: "2.0.0"

source:
  git_url: https://github.com/pinglu-zhang/HAlign-5.git
  # git_rev: v2.0.0

build:
  number: 4

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - make

  host:
    - zlib
    - libcurl
    - llvm-openmp
    - sysroot_linux-64 2.17

  run:
    - zlib
    - libcurl
    - llvm-openmp
    - mafft
    - minipoa
    - clustalo

test:
  commands:
    - halign5 -h
    - mafft --version
    - minipoa -h
    - clustalo --version

about:
  home: https://github.com/pinglu-zhang/HAlign-5
  license: MIT
  summary: "HAlign-5"
  description: "HAlign 4: A New Strategy for Rapidly Aligning Millions of Sequences"
  channels:
    - malab
    - bioconda
    - conda-forge
    - defaults
